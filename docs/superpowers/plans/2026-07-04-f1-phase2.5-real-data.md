# Phase 2.5: Real Data Aggregation - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Revive the app's data (Ergast is dead → switch to the free jolpica mirror), fix the broken standings endpoint, and aggregate multi-round / multi-season data so the Phase 2 analytics show real numbers instead of placeholders.

**Architecture:** Keep the existing service/Redux/component layers. Repoint the API base URL, fix the standings fetch to use the two correct endpoints, add aggregation methods to the API service (all rounds of a season; a driver's standings across N seasons), and rewrite the analytics service to compute stats/trends/head-to-head from that aggregated data. Cache aggressively to respect the free API's rate limits.

**Tech Stack:** Existing Phase 1/2 stack. Data source: `https://api.jolpi.ca/ergast/f1` (free, community Ergast replacement, identical JSON format).

## Global Constraints

- **Language:** TypeScript strict mode; `npm run type-check` must pass
- **Testing:** Jest (jest-expo preset); full suite must stay green
- **Free API only:** jolpica mirror; cache results, avoid redundant calls (rate limit ~500 req/hour, 4 req/sec burst)
- **Data source of truth:** jolpica endpoints verified working:
  - Races: `/{season}.json?limit=100`
  - Driver standings: `/{season}/driverStandings.json`
  - Constructor standings: `/{season}/constructorStandings.json`
  - Race results: `/{season}/{round}/results.json`
  - Season-wide results (paginated): `/{season}/results.json?limit=100&offset=N` (2024 = 479 rows → 5 pages)
  - Qualifying: `/{season}/{round}/qualifying.json`
- **Naming:** camelCase functions/vars, PascalCase components/types
- **No AI attribution** in commit messages

---

## Task 1: Repoint API + fix standings endpoints (critical hotfix)

**Files:**
- Modify: `src/utils/constants.ts` (base URL → jolpica — MAY ALREADY BE DONE in working tree)
- Modify: `src/services/ergastAPI.ts` (`getStandings`)
- Modify: `__tests__/unit/services/ergastAPI.test.ts` (update standings mock/expectations)

**Problem:** `ergast.com` is offline (HTTP 000). `getStandings` calls `/{season}/standings.json` which returns HTTP 400 — that combined endpoint does not exist. Driver and constructor standings live at separate endpoints.

**Fix:**
- Ensure `ERGAST_API_BASE_URL = 'https://api.jolpi.ca/ergast/f1'`
- Rewrite `getStandings(season, round?)` to fetch BOTH:
  - `GET /{season}/driverStandings.json` (or `/{season}/{round}/driverStandings.json`) → `MRData.StandingsTable.StandingsList[0].DriverStandings`
  - `GET /{season}/constructorStandings.json` (or with round) → `MRData.StandingsTable.StandingsList[0].ConstructorStandings`
  - Run them concurrently (Promise.all), merge into the existing `StandingsTable` return shape (season, round, driverStandings, constructorStandings)
- Update the unit test to mock two responses and assert both lists populate
- Verify: `npm run type-check`, run the ergastAPI test suite, and confirm the full suite stays green

**Commit:** `fix: switch to jolpica API mirror and fetch driver/constructor standings correctly`

---

## Task 2: Season results aggregation in the API service

**Files:**
- Modify: `src/services/ergastAPI.ts` (add `getSeasonResults`)
- Modify: `__tests__/unit/services/ergastAPI.test.ts`

**Add** `getSeasonResults(season: string): Promise<RaceResult[]>`:
- Paginate `GET /{season}/results.json?limit=100&offset=N` until all `MRData.total` rows are fetched (flatten every `Races[].Results[]`, attaching each race's round/raceName if useful)
- Reuse the existing `withRetry` wrapper
- Cache per-season in an in-memory Map so repeat calls don't re-hit the API
- Unit test with mocked paginated responses (e.g. total=3 across 1-2 pages) asserting all results flatten correctly

**Commit:** `feat: add paginated season results aggregation to api service`

---

## Task 3: Multi-season standings fetch for trends

**Files:**
- Modify: `src/services/ergastAPI.ts` (add `getDriverSeasonStandings`)

**Add** `getDriverSeasonStandings(driverId: string, seasons: string[]): Promise<Array<{ season: string; points: number; position: number; wins: number }>>`:
- For each season, `GET /{season}/driverStandings.json`, find the entry whose `driver.driverId === driverId`, extract points/position/wins
- Run seasons concurrently (Promise.all); tolerate a season where the driver has no entry (skip it)
- Cache per (driverId, seasons) key

**Commit:** `feat: add multi-season driver standings fetch for trends`

---

## Task 4: Real analytics from aggregated data

**Files:**
- Modify: `src/services/analyticsService.ts`

- `calculateDriverStats`: already computes from a `RaceResult[]` — ensure it works over a FULL season's results (from `getSeasonResults`), not a single race
- `getTrendData(driverId, seasonStandings)`: rewrite to build real `TrendData` (`seasons: string[]`, `values: number[]`, `metricType`, `trend`) from the multi-season standings array (Task 3). Compute `trend` ('improving'|'declining'|'stable') by comparing first vs last value for the selected metric.
- `compareDrivers`: compute a real head-to-head over a shared `RaceResult[]` set — for races where BOTH drivers finished, count who placed ahead (`racesMet`, `driver1Wins`, `driver2Wins`), plus poles/fastest laps from each driver's season results
- Keep method signatures backward-compatible where screens call them; if a signature must change, update the callers (DriverDetailScreen, HeadToHeadScreen, TrendAnalysisScreen, ConstructorAnalysisScreen)

**Commit:** `feat: compute real driver stats, trends, and head-to-head from aggregated data`

---

## Task 5: Wire screens to aggregated data

**Files:**
- Modify: `src/screens/driver-detail/DriverDetailScreen.tsx`
- Modify: `src/screens/head-to-head/HeadToHeadScreen.tsx`
- Modify: `src/screens/trend-analysis/TrendAnalysisScreen.tsx`
- Modify: `src/screens/constructor-analysis/ConstructorAnalysisScreen.tsx`
- Add: an analytics thunk or effect that fetches season results + multi-season standings before computing

- On mount, dispatch/fetch: `ergastService.getSeasonResults(selectedSeason)` and (for trends) `getDriverSeasonStandings(driverId, [n-2, n-1, n])`
- Feed the aggregated `RaceResult[]` into `calculateDriverStats` / `compareDrivers`, and the season-standings array into `getTrendData`
- Show `SkeletonLoader` during the (now-heavier) fetch; keep empty states
- Verify each screen renders real numbers

**Commit:** `feat: wire analytics screens to aggregated multi-round and multi-season data`

---

## Task 6: Verification

- `npm run type-check` — clean
- `npm test -- --watchAll=false` — full suite green
- `npx expo export --platform android` — bundles cleanly; then `rm -rf dist`
- Manual sanity: confirm a driver detail screen would show non-zero career stats and a trend line with 3 seasons
- Note any remaining rate-limit/caching considerations

**Commit:** (only if verification required code changes) `test: verify phase 2.5 real-data integration`

---

## Success Criteria

- App fetches real data again (jolpica), standings load (driver + constructor)
- Driver career stats reflect a full season's results, not one race
- Trend charts show a real multi-season line
- Head-to-head reflects true shared-race comparisons
- Type-check clean, full test suite green, Android bundle exports
- All API usage stays on the free jolpica mirror with caching
